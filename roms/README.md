# ROM inputs

The SD-image build requires `boot0.rom` and `boot1.rom`. These files are not
stored in this repository. Put them in this directory, or set `Z486_ROM_DIR`
to another directory containing both files before running an image-build
target.
